import type { RequestBody } from '@discordjs/rest/dist/lib/errors/DiscordAPIError';
import type { InternalRequest } from '@discordjs/rest/dist/lib/RequestManager';
/**
 * Represents a HTTP error
 */
export declare class HTTPError extends Error {
    name: string;
    status: number;
    method: string;
    url: string;
    requestBody: RequestBody;
    /**
     * @param name The name of the error
     * @param status The status code of the response
     * @param method The method of the request that erred
     * @param url The url of the request that erred
     * @param bodyData The unparsed data for the request that errored
     */
    constructor(name: string, status: number, method: string, url: string, bodyData: Pick<InternalRequest, 'files' | 'body'>);
}
