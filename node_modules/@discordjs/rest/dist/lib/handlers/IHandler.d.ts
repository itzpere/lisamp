import type { Dispatcher } from 'undici';
import type { RequestOptions } from '@discordjs/rest/dist/lib/REST';
import type { HandlerRequestData, RouteData } from '@discordjs/rest/dist/lib/RequestManager';
export interface IHandler {
    queueRequest: (routeId: RouteData, url: string, options: RequestOptions, requestData: HandlerRequestData) => Promise<Dispatcher.ResponseData>;
    get inactive(): boolean;
    readonly id: string;
}
